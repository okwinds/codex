# `sdk/typescript/src/threadOptions.ts`

## Identity
- kind: `source`
- ext: `.ts`
- size_bytes: `662`
- sha256: `e6c908c9f92a3bbbff1a7d3becdfc0669660dc5dc3fd3d00b15929e709fb5079`
- generated_utc: `2026-02-03T16:08:31Z`

## Purpose (Why)
Source file implementing exported/public items listed below.

## Interfaces (Inputs/Outputs)
### Inputs
- filesystem: `sdk/typescript/src/threadOptions.ts` (read)

### Outputs / Side Effects
- (no obvious side effects detected by heuristic)

## Public Surface (auto)
- `export type ApprovalMode = "never" | "on-request" | "on-failure" | "untrusted";`
- `export type SandboxMode = "read-only" | "workspace-write" | "danger-full-access";`
- `export type ModelReasoningEffort = "minimal" | "low" | "medium" | "high" | "xhigh";`
- `export type WebSearchMode = "disabled" | "cached" | "live";`
- `export type ThreadOptions = {`

## Definitions (auto, per-file)
- `export` `sdk/typescript/src/threadOptions.ts:1` `export type ApprovalMode = "never" | "on-request" | "on-failure" | "untrusted";`
- `export` `sdk/typescript/src/threadOptions.ts:3` `export type SandboxMode = "read-only" | "workspace-write" | "danger-full-access";`
- `export` `sdk/typescript/src/threadOptions.ts:5` `export type ModelReasoningEffort = "minimal" | "low" | "medium" | "high" | "xhigh";`
- `export` `sdk/typescript/src/threadOptions.ts:7` `export type WebSearchMode = "disabled" | "cached" | "live";`
- `export` `sdk/typescript/src/threadOptions.ts:9` `export type ThreadOptions = {`

## Dependencies (auto sample)
### Imports / Includes
- `export type ApprovalMode = "never" | "on-request" | "on-failure" | "untrusted";`
- `export type SandboxMode = "read-only" | "workspace-write" | "danger-full-access";`
- `export type ModelReasoningEffort = "minimal" | "low" | "medium" | "high" | "xhigh";`
- `export type WebSearchMode = "disabled" | "cached" | "live";`
- `export type ThreadOptions = {`
### Referenced env vars
- (none detected)

## Error Handling / Edge Cases
- (no obvious error-handling patterns detected by heuristic)

## Spec Links
- `workdocjcl/spec/00_Overview/PROJECT.md`

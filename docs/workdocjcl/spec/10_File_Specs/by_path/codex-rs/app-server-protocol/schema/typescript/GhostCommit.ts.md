# `codex-rs/app-server-protocol/schema/typescript/GhostCommit.ts`

## Identity
- kind: `source`
- ext: `.ts`
- size_bytes: `376`
- sha256: `c81968f60a53d8a9eba64e3c98aef91a6887307258276667f3cbedd343a6deba`
- generated_utc: `2026-02-08T10:45:14Z`

## Purpose (Why)
Source file implementing exported/public items listed below.

## Interfaces (Inputs/Outputs)
### Inputs
- filesystem: `codex-rs/app-server-protocol/schema/typescript/GhostCommit.ts` (read)

### Outputs / Side Effects
- (no obvious side effects detected by heuristic)

## Public Surface (auto)
- `export type GhostCommit = { id: string, parent: string | null, preexisting_untracked_files: Array<string>, preexisting_untracked_dirs: Array<string>, };`

## Definitions (auto, per-file)
- `export` `codex-rs/app-server-protocol/schema/typescript/GhostCommit.ts:8` `export type GhostCommit = { id: string, parent: string | null, preexisting_untracked_files: Array<string>, preexisting_untracked_dirs: Array<string>, };`

## Dependencies (auto sample)
### Imports / Includes
- `export type GhostCommit = { id: string, parent: string | null, preexisting_untracked_files: Array<string>, preexisting_untracked_dirs: Array<string>, };`
### Referenced env vars
- (none detected)

## Error Handling / Edge Cases
- (no obvious error-handling patterns detected by heuristic)

## Spec Links
- (none; see `09_Verification/CODE_TO_SPEC_MAP.md`)

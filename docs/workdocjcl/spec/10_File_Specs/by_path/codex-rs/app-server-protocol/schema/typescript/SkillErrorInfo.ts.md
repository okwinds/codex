# `codex-rs/app-server-protocol/schema/typescript/SkillErrorInfo.ts`

## Identity
- kind: `source`
- ext: `.ts`
- size_bytes: `218`
- sha256: `0d59892e9b1d002a1d7065a49b3951c3d9a8b73bd1746a5b76c3018882296a54`
- generated_utc: `2026-02-03T16:08:28Z`

## Purpose (Why)
Source file implementing exported/public items listed below.

## Interfaces (Inputs/Outputs)
### Inputs
- filesystem: `codex-rs/app-server-protocol/schema/typescript/SkillErrorInfo.ts` (read)

### Outputs / Side Effects
- (no obvious side effects detected by heuristic)

## Public Surface (auto)
- `export type SkillErrorInfo = { path: string, message: string, };`

## Definitions (auto, per-file)
- `export` `codex-rs/app-server-protocol/schema/typescript/SkillErrorInfo.ts:5` `export type SkillErrorInfo = { path: string, message: string, };`

## Dependencies (auto sample)
### Imports / Includes
- `export type SkillErrorInfo = { path: string, message: string, };`
### Referenced env vars
- (none detected)

## Error Handling / Edge Cases
- (no obvious error-handling patterns detected by heuristic)

## Spec Links
- (none; see `09_Verification/CODE_TO_SPEC_MAP.md`)

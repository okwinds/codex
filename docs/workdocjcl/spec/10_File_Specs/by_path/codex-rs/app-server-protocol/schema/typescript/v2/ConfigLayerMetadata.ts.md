# `codex-rs/app-server-protocol/schema/typescript/v2/ConfigLayerMetadata.ts`

## Identity
- kind: `source`
- ext: `.ts`
- size_bytes: `296`
- sha256: `bc6dac6f3c9ac7fa7d7a40c15abc4114cfe230f4777cabf8e0e130d70b78a515`
- generated_utc: `2026-02-03T16:08:28Z`

## Purpose (Why)
Source file implementing exported/public items listed below.

## Interfaces (Inputs/Outputs)
### Inputs
- filesystem: `codex-rs/app-server-protocol/schema/typescript/v2/ConfigLayerMetadata.ts` (read)

### Outputs / Side Effects
- (no obvious side effects detected by heuristic)

## Public Surface (auto)
- `export type ConfigLayerMetadata = { name: ConfigLayerSource, version: string, };`

## Definitions (auto, per-file)
- `import` `codex-rs/app-server-protocol/schema/typescript/v2/ConfigLayerMetadata.ts:4` `import type { ConfigLayerSource } from "./ConfigLayerSource";`
- `export` `codex-rs/app-server-protocol/schema/typescript/v2/ConfigLayerMetadata.ts:6` `export type ConfigLayerMetadata = { name: ConfigLayerSource, version: string, };`

## Dependencies (auto sample)
### Imports / Includes
- `import type { ConfigLayerSource } from "./ConfigLayerSource";`
- `export type ConfigLayerMetadata = { name: ConfigLayerSource, version: string, };`
### Referenced env vars
- (none detected)

## Error Handling / Edge Cases
- (no obvious error-handling patterns detected by heuristic)

## Spec Links
- (none; see `09_Verification/CODE_TO_SPEC_MAP.md`)

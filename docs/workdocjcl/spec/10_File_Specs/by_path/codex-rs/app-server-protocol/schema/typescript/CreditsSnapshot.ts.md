# `codex-rs/app-server-protocol/schema/typescript/CreditsSnapshot.ts`

## Identity
- kind: `source`
- ext: `.ts`
- size_bytes: `254`
- sha256: `a8570b834fcd2e901ea5dc66e25dc960cc6cc8d3b32092e22dc76d6d624854d3`
- generated_utc: `2026-02-08T10:45:14Z`

## Purpose (Why)
Source file implementing exported/public items listed below.

## Interfaces (Inputs/Outputs)
### Inputs
- filesystem: `codex-rs/app-server-protocol/schema/typescript/CreditsSnapshot.ts` (read)

### Outputs / Side Effects
- (no obvious side effects detected by heuristic)

## Public Surface (auto)
- `export type CreditsSnapshot = { has_credits: boolean, unlimited: boolean, balance: string | null, };`

## Definitions (auto, per-file)
- `export` `codex-rs/app-server-protocol/schema/typescript/CreditsSnapshot.ts:5` `export type CreditsSnapshot = { has_credits: boolean, unlimited: boolean, balance: string | null, };`

## Dependencies (auto sample)
### Imports / Includes
- `export type CreditsSnapshot = { has_credits: boolean, unlimited: boolean, balance: string | null, };`
### Referenced env vars
- (none detected)

## Error Handling / Edge Cases
- (no obvious error-handling patterns detected by heuristic)

## Spec Links
- (none; see `09_Verification/CODE_TO_SPEC_MAP.md`)
